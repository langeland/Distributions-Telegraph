Neos:
  Flow:
    persistence:
      backendOptions:
        host: 'mysql'
        driver: 'pdo_mysql'
        dbname: %ENV_MYSQL_DATABASE%
        user: %ENV_MYSQL_USER%
        password: %ENV_MYSQL_PASSWORD%
